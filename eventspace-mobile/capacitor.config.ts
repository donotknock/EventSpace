import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eventspace.companion',
  appName: 'EventSpace Mobile',
  webDir: 'dist'
};

export default config;
